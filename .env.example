DATABASE_URL=postgresql://deckpipe:deckpipe@localhost:5433/deckpipe
PORT=3001
API_URL=http://localhost:3001
VIEWER_URL=http://localhost:5173
IMAGE_STORAGE_PATH=./data/images
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
# PostHog (optional overrides — defaults to the shared Purple Rain Labs EU project)
# POSTHOG_KEY=phc_uUq5DaCoF5SpGeXo353jR3uMenhy75QFgv6NUpzCzTPg
# POSTHOG_HOST=https://eu.i.posthog.com
# POSTHOG_DISABLED=1

# Headless renderer (Puppeteer). Chromium is launched on the first render and
# closed again after this many ms without one — idle Chromium is ~1 GB RSS.
# RENDER_BROWSER_IDLE_MS=180000
# Max concurrent renders; extra callers queue FIFO.
# RENDER_CONCURRENCY=2
